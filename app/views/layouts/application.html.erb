<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Town Record Keeper" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class='row'>
      <div>
        <h1>Town Records Keeper

        </h1>
      <%= link_to 'Birth Records', birth_records_path, class: "button right center small" %>
      <%= link_to 'Death Records', death_records_path, class: "button right center small" %>
      </div>
    </div>

    <%= notice %>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
